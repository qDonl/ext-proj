<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.donl.health.mapper.AccountMapper">

<select id="findById" resultType="top.donl.health.model.po.Account">
    select *
    from `account`
    where id = #{accountId}
    </select>
</mapper>